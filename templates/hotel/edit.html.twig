{% extends 'base.html.twig' %}

{% block title %}Edit Hotel{% endblock %}

{% block body %}
    <h1>Edit Hotel</h1>

    {{ include('hotel/formEdite.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('hotel_index') }}">back to list</a>

    {{ include('hotel/_delete_form.html.twig') }}
{% endblock %}
